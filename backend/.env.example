# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Backend server port
PORT=50010

# Note: The new multi-task transformer model does NOT require API keys
# Old dependencies (Gemini, HuggingFace) have been removed
# The model runs entirely locally using XLM-RoBERTa + Whisper
